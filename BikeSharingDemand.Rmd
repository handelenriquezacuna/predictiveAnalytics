---
title: "BikeSharingDemand"
output: html_document
authors: Charpentier Gamboa Eduardo Alonso, Enríquez Acuña Handel y Hidalgo Guevara Daniel Josué
---
```{r instalacion de librerias}
library(xgboost)
library(readr)
library(tidyverse)
library(janitor)
library(skimr)
library(kableExtra)
library(patchwork)
library(caret)
library(randomForest)
library(rpart)
library(rpart.plot)
```

```{r Cargamos las librerías necesarias}
library(readr)      # Para leer el archivo csv
library(tidyverse)  # Para usar select, filter, mutate y pipes (%>%)
library(janitor)    # Para limpiar los nombres de las columnas rápidamente
library(skimr)      # Para el resumen estadístico detallado
library(kableExtra) # Para que nuestras tablas se vean ordenadas en el reporte
```
# 2. Descripción del Problema

Los sistemas de bicicletas compartidas se han convertido en una pieza fundamental de la
movilidad urbana sostenible. Predecir con precisión la demanda diaria de alquiler permite
a los operadores **optimizar la distribución de flotas**, planificar mantenimiento y mejorar
la experiencia del usuario.

La pregunta central de este proyecto es:

> **¿Cuántas bicicletas serán alquiladas en un día determinado, considerando factores
climáticos y temporales?**

Un modelo predictivo robusto permite:

- **Evitar desabastecimiento** en días de alta demanda (p. ej., días cálidos y secos).
- **Reducir costos operativos** redistribuyendo unidades en días de baja demanda.
- **Planificar estratégicamente** según estaciones del año, feriados y condiciones climáticas.

# Presentacion del dataset
```{r Cargamos el dataset}
df <- read_csv("recursos/day.csv")
glimpse(df)
df <- as_tibble(df)
df
```
> **Variable objetivo:** `cnt` — total de bicicletas alquiladas por día.
>
> **Nota:** `casual` y `registered` no deben usarse como predictores ya que su suma
> compone directamente `cnt`.

## Renombrar y tipificar columnas
```{r}
df_clean <- df %>%
  rename(
    fecha        = dteday,
    estacion     = season,
    anio         = yr,
    mes          = mnth,
    feriado      = holiday,
    dia_semana   = weekday,
    dia_laboral  = workingday,
    clima        = weathersit,
    total_rentas = cnt
  ) %>%
  mutate(
    fecha       = as.Date(fecha),
    estacion    = factor(estacion, levels = 1:4,
                         labels = c("Invierno", "Primavera", "Verano", "Otoño")),
    anio        = factor(anio, levels = 0:1, labels = c("2011", "2012")),
    mes         = factor(mes, levels = 1:12, labels = month.abb),
    feriado     = factor(feriado, levels = 0:1, labels = c("No", "Sí")),
    dia_semana  = factor(dia_semana, levels = 0:6,
                         labels = c("Dom", "Lun", "Mar", "Mié", "Jue", "Vie", "Sab")),
    dia_laboral = factor(dia_laboral, levels = 0:1, labels = c("No", "Sí")),
    clima       = factor(clima, levels = 1:4,
                         labels = c("Despejado", "Nublado", "Lluvia Ligera", "Tormenta")),
    tipo_dia    = ifelse(dia_laboral == "Sí", "Día Laboral", "Fin de Semana / Feriado"),
    pct_casual  = round((casual / total_rentas) * 100, 1)
  )

glimpse(df_clean)
```
## Verificación de valores faltantes

```{r valores-faltantes}
na_summary <- df_clean %>%
  summarise(across(everything(), ~ sum(is.na(.)))) %>%
  pivot_longer(everything(), names_to = "Variable", values_to = "NA_Count") %>%
  filter(NA_Count > 0)

if (nrow(na_summary) == 0) {
  cat("No se encontraron valores faltantes en ninguna variable.\n")
} else {
  na_summary %>%
    kable() %>%
    kable_styling(bootstrap_options = c("striped"), full_width = FALSE)
}
```
## Resumen estadístico
```{r resumen-estadistico}
df_clean %>%
  select(temp, atemp, hum, windspeed, casual, registered, total_rentas) %>%
  skim() %>%
  kable() %>%
  kable_styling(bootstrap_options = c("striped", "hover", "condensed"),
                full_width = FALSE, font_size = 12)
```

# Análisis Exploratorio de Datos (EDA)

## Distribución de la variable objetivo (`total_rentas`)
```{r dist-total-rentas}
stats_cnt <- df_clean %>%
  summarise(
    Media   = round(mean(total_rentas)),
    Mediana = median(total_rentas),
    SD      = round(sd(total_rentas)),
    Min     = min(total_rentas),
    Max     = max(total_rentas),
    Q1      = quantile(total_rentas, 0.25),
    Q3      = quantile(total_rentas, 0.75)
  )

stats_cnt %>%
  kable(caption = "Estadísticas descriptivas de la demanda diaria") %>%
  kable_styling(bootstrap_options = c("striped"), full_width = FALSE)

ggplot(df_clean, aes(x = total_rentas)) +
  geom_histogram(aes(y = after_stat(density)),
                 bins = 30, fill = "#3498db", color = "white", alpha = 0.8) +
  geom_density(color = "#e74c3c", linewidth = 1) +
  geom_vline(xintercept = mean(df_clean$total_rentas),
             linetype = "dashed", color = "#2c3e50", linewidth = 0.8) +
  annotate("text",
           x = mean(df_clean$total_rentas) + 300,
           y = 0.00035,
           label = paste0("Media = ", round(mean(df_clean$total_rentas))),
           hjust = 0, fontface = "bold", size = 3.5) +
  scale_y_continuous(labels = scales::label_number(accuracy = 0.00001)) +
  labs(
    title = "Distribución de la Demanda Diaria de Bicicletas",
    subtitle = "Histograma con curva de densidad superpuesta",
    x = "Total de Bicicletas Alquiladas por Día",
    y = "Densidad"
  ) +
  theme_minimal(base_size = 13)
```

La distribución muestra una forma aproximadamente **normal con leve asimetría negativa**,
lo que sugiere que la mayoría de los días tienen demanda moderada-alta, con
algunos días atípicos de baja demanda (posiblemente asociados a condiciones climáticas adversas).

## Detección de Outliers
```{r outliers}
p_box <- ggplot(df_clean, aes(y = total_rentas)) +
  geom_boxplot(fill = "#3498db", alpha = 0.6, width = 0.4) +
  labs(title = "Boxplot de Demanda Diaria", y = "Total Rentas") +
  theme_minimal(base_size = 13) +
  theme(axis.text.x = element_blank(), axis.ticks.x = element_blank())

Q1 <- quantile(df_clean$total_rentas, 0.25)
Q3 <- quantile(df_clean$total_rentas, 0.75)
IQR_val <- Q3 - Q1
lower <- Q1 - 1.5 * IQR_val
upper <- Q3 + 1.5 * IQR_val

outliers <- df_clean %>%
  filter(total_rentas < lower | total_rentas > upper)

cat(paste0("Rango IQR: [", round(lower), ", ", round(upper), "]\n"))
cat(paste0("Outliers detectados: ", nrow(outliers), " días\n"))

p_box_season <- ggplot(df_clean, aes(x = estacion, y = total_rentas, fill = estacion)) +
  geom_boxplot(alpha = 0.7) +
  scale_fill_brewer(palette = "Set2") +
  labs(title = "Distribución por Estación", x = "", y = "Total Rentas") +
  theme_minimal(base_size = 13) +
  theme(legend.position = "none")

p_box | p_box_season
```
```{r outliers-detalle}
if (nrow(outliers) > 0) {
  outliers %>%
    select(fecha, estacion, clima, temp, hum, windspeed, total_rentas) %>%
    arrange(total_rentas) %>%
    kable(caption = "Días identificados como outliers") %>%
    kable_styling(bootstrap_options = c("striped", "hover"), full_width = FALSE)
}
```

## Patrones Temporales
### Evolucion temporal de la demanda

Se utiliza LOESS(LOcally Estimated Scatterplot Smoothing) para la reduccion de ruido y revelar la tendencia en la demanda

```{r serie-temporal}
ggplot(df_clean, aes(x = fecha, y = total_rentas)) +
  geom_line(color = "#7f8c8d", alpha = 0.5) +
  geom_smooth(method = "loess", span = 0.15, color = "#e74c3c", se = TRUE, alpha = 0.2) +
  labs(
    title = "Evolución Diaria de la Demanda de Bicicletas (2011–2012)",
    subtitle = "Línea suavizada LOESS en rojo",
    x = "Fecha",
    y = "Total de Bicicletas Alquiladas"
  ) +
  scale_x_date(date_labels = "%b %Y", date_breaks = "2 months") +
  theme_minimal(base_size = 13) +
  theme(axis.text.x = element_text(angle = 45, hjust = 1))
```
Se observa una **clara estacionalidad anual**: la demanda crece de primavera a verano/otoño
y disminuye en invierno. Además, hay una **tendencia creciente** entre 2011 y 2012,
posiblemente por mayor adopción del sistema.


visualizaciones
```{r demanda por mes}

demanda_mes <- df_clean %>%
  group_by(mes) %>%
  summarise(
    promedio_rentas = mean(total_rentas),
    .groups = "drop"
  )

ggplot(demanda_mes, aes(x = mes, y = promedio_rentas, group = 1)) +
  geom_line(color = "#2c3e50", linewidth = 1) +
  geom_point(size = 3, color = "#e74c3c") +
  labs(
    title = "Demanda Promedio de Bicicletas por Mes",
    x = "Mes",
    y = "Promedio de Bicicletas Alquiladas"
  ) +
  theme_minimal(base_size = 13)

```

Se puede observar que entre los meses de mayo a septiembre es cuando hay mayor demanda de bicicletas, se interpreta la razón de este suceso por e hecho de la entrada del verano(mayo), el verano (junio- agosto), y la salida del verano(septiembre), y ya en meses o epocas más frías la demanda disminuye

```{r dia laboral vs fin de semana}

demanda_tipo_dia <- df_clean %>%
  group_by(tipo_dia) %>%
  summarise(
    promedio_rentas = mean(total_rentas),
    .groups = "drop"
  )

ggplot(df_clean, aes(x = tipo_dia, y = total_rentas, fill = tipo_dia)) +
  geom_boxplot(alpha = 0.7) +
  scale_fill_brewer(palette = "Set2") +
  labs(
    title = "Demanda: Día Laboral vs Fin de Semana / Feriado",
    x = "",
    y = "Total de Bicicletas Alquiladas"
  ) +
  theme_minimal(base_size = 13) +
  theme(legend.position = "none")

```
Se puede interpretar que el limite superior del día laboral es ligeramente mayor, mientras que el limite inferior de fin de semana es altamente inferior, lo cual indica más variedad en la demanda de bicicletas

```{r demanda vs temperatura}

ggplot(df_clean, aes(x = temp, y = total_rentas)) +
  geom_point(alpha = 0.5, color = "#3498db") +
  geom_smooth(method = "lm", color = "#e74c3c", se = TRUE) +
  labs(
    title = "Relación entre Temperatura y Demanda de Bicicletas",
    x = "Temperatura (Normalizada)",
    y = "Total de Bicicletas Alquiladas"
  ) +
  theme_minimal(base_size = 13)

```
Se puede observar que la temperatura afecta la cantidad de bicicletas alquiladas, ya que se puede observar una mayor cantidad de alquileres mientras más alta sea la temperatura

# Preparación del dataset

## Limpieza final

Antes de construir cualquier modelo es necesario dejar el dataframe libre de columnas
redundantes, registros duplicados y variables que filtren información de la variable
objetivo.

```{r limpieza-final}
# 1. Eliminar columnas que no aportan al modelo
#    - instant : índice secuencial sin valor predictivo
#    - casual / registered : su suma ES la variable objetivo (fuga de datos)
#    - pct_casual : derivada de casual y total_rentas (también fuga)
df_model <- df_clean %>%
  select(-instant, -casual, -registered, -pct_casual)

cat("Dimensiones después de eliminar columnas redundantes:",
    nrow(df_model), "filas ×", ncol(df_model), "columnas\n")
```

```{r duplicados}
# 2. Verificar y eliminar registros duplicados
n_dup <- df_model %>% duplicated() %>% sum()
cat("Registros duplicados encontrados:", n_dup, "\n")

df_model <- df_model %>% distinct()
cat("Dimensiones tras eliminar duplicados:",
    nrow(df_model), "filas ×", ncol(df_model), "columnas\n")
```

```{r verificacion-tipos}
# 3. Verificación de tipos de datos
df_model %>%
  summarise(across(everything(), ~ class(.)[1])) %>%
  pivot_longer(everything(), names_to = "Variable", values_to = "Tipo") %>%
  kable(caption = "Tipos de datos finales por variable") %>%
  kable_styling(bootstrap_options = c("striped", "hover"), full_width = FALSE)
```

## Feature engineering con fechas usando lubridate

A partir de la columna *fecha* se extraen componentes temporales que capturan
patrones cíclicos de demanda. Se utiliza **codificación cíclica** (seno / coseno)
para que el modelo interprete correctamente la naturaleza circular del calendario
(por ejemplo, diciembre y enero están "cerca" en el ciclo anual).

Ejemplo en python: https://medium.com/@axelazara6/why-we-need-encoding-cyclical-features-79ecc3531232

```{r feature-eng-fechas, message=FALSE}
library(lubridate)

df_model <- df_model %>%
  mutate(
    # ── Componentes directos ──
    dia_del_anio   = yday(fecha),
    semana_del_anio = isoweek(fecha),
    trimestre      = quarter(fecha),
    es_inicio_mes  = if_else(day(fecha) <= 5, 1L, 0L),
    es_fin_mes     = if_else(day(fecha) >= 25, 1L, 0L),

    # ── Codificación cíclica del mes (período = 12) ──
    mes_sin = sin(2 * pi * as.numeric(mes) / 12),
    mes_cos = cos(2 * pi * as.numeric(mes) / 12),

    # ── Codificación cíclica del día de la semana (período = 7) ──
    dia_sem_sin = sin(2 * pi * as.numeric(dia_semana) / 7),
    dia_sem_cos = cos(2 * pi * as.numeric(dia_semana) / 7),

    # ── Codificación cíclica del día del año (período = 365) ──
    dia_anio_sin = sin(2 * pi * dia_del_anio / 365),
    dia_anio_cos = cos(2 * pi * dia_del_anio / 365)
  )

cat("Nuevas features temporales creadas.\n")
cat("Dimensiones actualizadas:",
    nrow(df_model), "filas ×", ncol(df_model), "columnas\n")
```

```{r vista-features-fecha}
df_model %>%
  select(fecha, dia_del_anio, semana_del_anio, trimestre,
         es_inicio_mes, es_fin_mes,
         mes_sin, mes_cos, dia_sem_sin, dia_sem_cos) %>%
  head(10) %>%
  kable(digits = 3,
        caption = "Primeras 10 filas — Features derivadas de fecha") %>%
  kable_styling(bootstrap_options = c("striped", "hover", "condensed"),
                full_width = FALSE, font_size = 12)
```

### Visualización de la codificación cíclica

```{r viz-ciclica, fig.width=9, fig.height=4}
p_mes <- ggplot(df_model, aes(x = mes_sin, y = mes_cos, color = estacion)) +
  geom_point(size = 2, alpha = 0.6) +
  scale_color_brewer(palette = "Set1") +
  coord_equal() +
  labs(title = "Codificación Cíclica del Mes",
       x = "sin(mes)", y = "cos(mes)", color = "Estación") +
  theme_minimal(base_size = 12)

p_dia <- ggplot(df_model, aes(x = dia_anio_sin, y = dia_anio_cos, color = estacion)) +
  geom_point(size = 1.5, alpha = 0.5) +
  scale_color_brewer(palette = "Set1") +
  coord_equal() +
  labs(title = "Codificación Cíclica del Día del Año",
       x = "sin(día)", y = "cos(día)", color = "Estación") +
  theme_minimal(base_size = 12)

p_mes + p_dia + plot_layout(guides = "collect") &
  theme(legend.position = "bottom")
```

La distribución circular confirma que **las estaciones se agrupan en arcos del
círculo**, lo cual permite al modelo capturar la periodicidad sin discontinuidades
artificiales (como pasar de diciembre = 12 a enero = 1).

## Codificación de variables categóricas

Para que los algoritmos de regresión trabajen correctamente con las variables
categóricas, se aplica **one-hot encoding** (variables dummy). Se excluye una
categoría de referencia por variable para evitar multicolinealidad perfecta
(trampa de las variables dummy).

```{r one-hot-encoding}
# Variables categóricas presentes
cat_vars <- df_model %>%
  select(where(is.factor)) %>%
  names()

cat("Variables categóricas a codificar:", paste(cat_vars, collapse = ", "), "\n\n")

# Niveles por variable
for (v in cat_vars) {
  cat(v, "→", nlevels(df_model[[v]]), "niveles:",
      paste(levels(df_model[[v]]), collapse = ", "), "\n")
}
```

```{r dummy-encoding}
# One-hot encoding con model.matrix (drop first = categoría de referencia)
df_encoded <- df_model %>%
  select(-fecha) %>%                        # fecha ya fue explotada en features
  mutate(across(where(is.factor), as.factor))

dummy_matrix <- model.matrix(~ . - 1, data = df_encoded)

# Convertir a tibble limpio
df_final <- as_tibble(dummy_matrix) %>%
  janitor::clean_names()

cat("\nDimensiones del dataset final (one-hot):",
    nrow(df_final), "filas ×", ncol(df_final), "columnas\n")
```

```{r vista-dummy}
df_final %>%
  head(8) %>%
  kable(digits = 3,
        caption = "Primeras 8 filas del dataset codificado (one-hot)") %>%
  kable_styling(bootstrap_options = c("striped", "hover", "condensed"),
                full_width = FALSE, font_size = 11) %>%
  scroll_box(width = "100%")
```

```{r resumen-final}
cat("══════════════════════════════════════════════\n")
cat("  RESUMEN DE PREPARACIÓN DEL DATASET\n")
cat("══════════════════════════════════════════════\n")
cat("  Filas               :", nrow(df_final), "\n")
cat("  Columnas            :", ncol(df_final), "\n")
cat("  Variables numéricas :", sum(sapply(df_final, is.numeric)), "\n")
cat("  Valores faltantes   :", sum(is.na(df_final)), "\n")
cat("══════════════════════════════════════════════\n")
cat("  Dataset listo para modelado predictivo.\n")
```


#División de entrenamiento /Prueba 
```{r}


set.seed(123)

#Creación de una partición del 70% para el entrenamiento
trainIndex <- createDataPartition(df_final$total_rentas, p = 0.7, list = FALSE)

train <- df_final[trainIndex, ]
test  <- df_final[-trainIndex, ]
```

#Modelo 1 
#Este modelo tiene el objectivo hacer una indentificación y cunatificación
#Basado en la influencia presente de los factores climaticos y temporales
#En la demanda de bicicletas diarias

#Ajuste del modelo 

#Que explique el total_rentas
#Buscamos aislar el impacto de variables criticas como son el clima 
#De las varaibles del crecimiento del negocio como lo es el año 2012


```{r}
modelo_1 <- lm(total_rentas ~ anio2012 + 
               temp + 
               atemp + 
               hum + 
               windspeed + 
               estacion_primavera + 
               estacion_verano + 
               estacion_otono +
               clima_nublado + 
               clima_lluvia_ligera +
               mes_sin + 
               mes_cos, 
               data = train)
summary(modelo_1)
```
#Evaluacion predictiva del primer modelo 
```{r}

# Realizar las predicciones usando el set de prueba (test)
pred <- predict(modelo_1, test)
RMSE <- sqrt(mean((test$total_rentas - pred)^2))
MAE  <- mean(abs(test$total_rentas - pred))
R2 <- cor(test$total_rentas, pred)^2
```


```{r}
MAE
```


#Si sumamos todas las diferencias entre lo real y lo predich
o (sin importar si sobraron o faltaron), el error típico es de 603 bicicletas.
Al ser menor que el RMSE, nos confirma que el modelo es bastante estable y
no tiene errores "extremos" o muy locos que arruinen el promedio.

```{r}
R2
```

#Interpretación:

#Es la métrica estrella. El modelo logra explicar el 82.48% de la variabilidad 
de la demanda en el set de prueba. Que este valor sea tan alto 
(y cercano al de entrenamiento)
confirma que no hay sobreajuste (overfitting) y que el modelo generaliza con éxito.

```{r}
RMSE
```
# Interpretación

#En promedio, el modelo se equivoca por 814 bicicletas. Esto quiere decir que 
si el modelo predice 4,000 rentas, el valor real probablemente estará entre 
3,186 y 4,814. Para un negocio que mueve miles de unidades, un error de 814 
es un margen aceptable y manejable.

#CONCLUSIÓN: 
#El modelo presenta un ajuste de alta calidad. La cercanía de los puntos 
a la línea roja confirma su robustez predictiva


#Modelo 2 

#Objectivo del modelo:

#Haremos una evaluación con un modelo con menos variables (más ahorrativo)
#Mantiene la capacidad predictiva del modelo 1, eliminando el ruido estadístico

# En este modelo incluimos solo los "Top Predictors" del Modelo 1:
# - anio2012 (Crecimiento)
# - atemp (Sensación térmica - que fue más relevante que temp)
# - hum y windspeed (Factores climáticos de castigo)
# - clima y estaciones (Contexto ambiental)

```{r}
modelo_2 <- lm(total_rentas ~ anio2012 + 
               atemp + 
               hum + 
               windspeed + 
               estacion_primavera + 
               estacion_otono +
               clima_nublado + 
               clima_lluvia_ligera, 
               data = train)


summary(modelo_2)
```


#Evaluación predictiva del segundo modelo

```{r}

pred_2 <- predict(modelo_2, test)


RMSE2   <- sqrt(mean((test$total_rentas - pred_2)^2))
MAE2    <- mean(abs(test$total_rentas - pred_2))
R2_2 <- cor(test$total_rentas, pred_2)^2
```

```{r}
RMSE2
```
#Interpretación:

#Pérdida de Precisión: Al quitar variables como los meses (mes_sin, mes_cos) 
o la temperatura real, el error subió en unas 49 bicicletas. 
El modelo ahora es un poco menos exacto, pero más simple.


```{r}

R2
```
#Interpretación:

#El modelo simplificado sigue explicando el 80.30% de la demanda. 
Es un modelo robusto, pero perdimos un 2.18% de precisión al eliminar variables.

```{r}
MAE2
```
#Interpretación:

#El error absoluto promedio subió en 37 bicicletas. Esto significa que, en un
día típico, el Modelo 2 se aleja más de la realidad que el Modelo 1.

#Conclusiones del modelo 2:

Aunque el Modelo 2 cumple con el principio de ahorrativo 
(usar menos variables para facilitar la interpretación), en este caso específico
la simplificación no compensa la pérdida de precisión.
En un negocio de logística como Bike Sharing, una diferencia de 814 vs 863 en el
RMSE puede significar una gestión de inventario menos eficiente.


# Modelo 3
#


```{r}

set.seed(123)

modelo_3 <- rpart(
  total_rentas ~ .,
  data = train,
  method = "anova",
  control = rpart.control(cp = 0.001)
)

summary(modelo_3)

```

```{r}
rpart.plot(modelo_3,
           type = 2,
           extra = 101,
           fallen.leaves = TRUE,
           main = "Árbol de decisión para demanda de bicicletas")

```


```{r}
# Realizar las predicciones usando el set de prueba (test)
pred_3 <- predict(modelo_3, test)

RMSE3 <- sqrt(mean((test$total_rentas - pred_3)^2))
MAE3  <- mean(abs(test$total_rentas - pred_3))
R2_3  <- cor(test$total_rentas, pred_3)^2

RMSE3
MAE3
R2_3
```

```{r}
comparacion <- tibble(
  Modelo = c("Modelo 1 (Regresión lineal completa)",
             "Modelo 2 (Regresión lineal simplificada)",
             "Modelo 3 (Árbol de decisión)"),
  
  RMSE = c(RMSE, RMSE2, RMSE3),
  MAE  = c(MAE,  MAE2,  MAE3),
  R2 = c(R2, R2, R2_3)
)

comparacion %>%
  kable(digits = 2, caption = "Comparación de desempeño de modelos") %>%
  kable_styling(full_width = FALSE)
```


#Interpretación

#El error absoluto promedio subió 46.24 bicicletas

#Conclusiones modelo 3

El árbol de desiciones logra capturar mejor los cambios bruscos de demanda que dependen de condiciones específicas (clima, estación, temperatura, etc.), algo que los modelos lineales no pueden representar fácilmente.
Sin embargo, este tipo de modelo puede volverse más inestable y sensible a los datos.
En un negocio como alquiler de bicicletas, una mejora en el error de predicción puede traducirse o interpretarse en una mejor planificación del número de bicicletas disponibles cada día, reduciendo faltantes o excesos.

# Modelo Final Seleccionado

## Modelos evaluados

Para predecir la demanda diaria de alquiler de bicicletas se entrenaron y compararon tres modelos de regresión supervisada de creciente complejidad:

- **Modelo 1 — Regresión lineal completa:** 12 predictores incluyendo variables climáticas (`temp`, `atemp`, `hum`, `windspeed`), estacionales (estaciones del año, codificación cíclica del mes) y de crecimiento (`anio2012`).
- **Modelo 2 — Regresión lineal simplificada:** versión reducida del Modelo 1 que conserva únicamente los predictores más significativos, bajo el principio de parsimonia.
- **Modelo 3 — Árbol de decisión CART:** modelo no lineal basado en particiones recursivas, con parámetro de complejidad `cp = 0.001` para permitir un crecimiento detallado del árbol.

Todos los modelos fueron evaluados sobre el **mismo set de prueba** (30% de los datos, 219 observaciones) separado con `set.seed(123)` y `createDataPartition()`, garantizando una comparación justa.

## Modelo seleccionado

**El modelo seleccionado como ganador es el Modelo 1 — Regresión lineal completa.**

### Justificación técnica

1. **Mejor desempeño cuantitativo.** Obtuvo el menor RMSE (814.33), el menor MAE (603.46) y el mayor R² (0.82) entre los tres modelos evaluados. La diferencia frente al Modelo 2 simplificado es de aproximadamente 49 bicicletas de RMSE, y frente al árbol de decisión de 104 bicicletas.

2. **Capacidad explicativa sólida.** Explica el 82% de la variabilidad de la demanda diaria sobre el set de prueba, un nivel consistente con el que se observó en entrenamiento, lo que confirma que no hay sobreajuste y el modelo generaliza correctamente.

3. **Superioridad sobre un modelo no lineal.** El hecho de que la regresión lineal supere al árbol de decisión indica que las relaciones dominantes entre las variables climáticas/temporales y la demanda son aproximadamente lineales y monótonas dentro del rango observado. Un árbol único, al particionar el espacio en bloques discretos, pierde la suavidad de estas relaciones y genera predicciones escalonadas menos precisas.

4. **Interpretabilidad directa.** A diferencia del árbol, la regresión lineal entrega coeficientes numéricos que traducen directamente el efecto de cada variable sobre la demanda (ej: cuánto suma un punto normalizado de temperatura). Esta transparencia es crítica para la comunicación con stakeholders del negocio y facilita las recomendaciones operativas de la Sección 5.

5. **Estabilidad del modelo.** Los coeficientes de una regresión lineal son deterministas dado el set de entrenamiento, mientras que los árboles son sensibles a pequeños cambios en los datos. Para un sistema de planificación diaria, la estabilidad es un atributo deseable.

## Parámetros principales del modelo seleccionado

El Modelo 1 no requiere tuning de hiperparámetros — es una regresión lineal múltiple por mínimos cuadrados ordinarios (OLS). La especificación final incluye 12 predictores seleccionados por el equipo en base al análisis exploratorio:

| Categoría | Variables |
|---|---|
| Crecimiento del negocio | `anio2012` |
| Clima continuo | `temp`, `atemp`, `hum`, `windspeed` |
| Estación del año | `estacion_primavera`, `estacion_verano`, `estacion_otono` |
| Condición climática | `clima_nublado`, `clima_lluvia_ligera` |
| Estacionalidad cíclica | `mes_sin`, `mes_cos` |

**Configuración técnica:**

- Método de estimación: mínimos cuadrados ordinarios (`lm()` de R base)
- Split train/test: 70/30 con `createDataPartition()` estratificada
- Semilla de reproducibilidad: `set.seed(123)`
- Categorías de referencia omitidas para evitar multicolinealidad: `estacion_invierno`, `clima_despejado`, `anio2011`

---

# Métricas Finales

## Evaluación sobre set de prueba

Los tres modelos fueron evaluados con las siguientes métricas estándar de regresión:

- **RMSE (Root Mean Squared Error):** penaliza errores grandes de forma cuadrática; unidad comparable a la variable objetivo (bicicletas).
- **MAE (Mean Absolute Error):** error promedio absoluto; robusto frente a outliers.
- **R² (Coeficiente de determinación):** proporción de la varianza explicada por el modelo.

## Tabla comparativa final

| Modelo | RMSE | MAE | R² |
|---|---|---|---|
| **Modelo 1 (Regresión lineal completa)** ⭐ | **814.33** | **603.46** | **0.82** |
| Modelo 2 (Regresión lineal simplificada) | 863.42 | 640.12 | 0.80 |
| Modelo 3 (Árbol de decisión) | 918.07 | 686.36 | 0.78 |

##  Interpretación cuantitativa de las métricas del modelo ganador


**RMSE = 814.33 bicicletas.** En promedio, las predicciones del Modelo 1 se desvían de la demanda real por ~814 unidades. Sobre una demanda promedio diaria de ~4,504 bicicletas, esto representa un margen de error relativo del **18%**. Para un negocio de logística urbana que mueve miles de unidades, es un margen operativamente manejable.

**MAE = 603.46 bicicletas.** El error absoluto promedio es menor que el RMSE, lo que indica que el modelo no produce errores extremos que inflen artificialmente el RMSE. Las predicciones son estables y consistentes día a día.

**R² = 0.82.** El modelo explica el 82% de la variabilidad de la demanda diaria usando únicamente variables climáticas y temporales. El 18% restante corresponde a factores no capturados por el dataset: eventos especiales, cambios de infraestructura, variabilidad individual de usuarios, entre otros.

**Relación RMSE/MAE ≈ 1.35.** Un cociente cercano a 1.25-1.4 es característico de errores bien distribuidos sin colas extremas. Confirma que el modelo no tiene días catastróficamente mal predichos.

#Gráfico de predicción:
```{r}
predicciones_m1 <- predict(modelo_1, newdata = test)

# Tabla de comparación 
df_evaluacion <- data.frame(
  Real = test$total_rentas,
  Predicho = predicciones_m1
)

# 3. Gráfico de dispersión 
library(ggplot2)

ggplot(df_evaluacion, aes(x = Real, y = Predicho)) +
  geom_point(color = "steelblue") +
  geom_abline(intercept = 0, slope = 1, color = "red") + 
  labs(
    title = "Predicción vs Valores Reales",
    x = "Valores Reales",
    y = "Predicciones"
  ) +
  theme_minimal()

```





##  Resumen ejecutivo de métricas

| Métrica | Valor | Lectura operativa |
|---|---|---|
| RMSE | 814 bicicletas | Margen típico de planificación diaria |
| MAE | 603 bicicletas | Error del día promedio |
| R² | 82% | Porcentaje de la demanda explicable por clima y calendario |
| Error relativo | ~18% | Sobre una demanda media de 4,504 bicicletas/día |

## Nota sobre el alcance del análisis

Elegir el Modelo 1 no implica que los modelos no lineales sean inadecuados en general para este problema — solo que, con este dataset y esta configuración, un árbol único no logra superar a una regresión lineal bien especificada. Si en el futuro el equipo explora modelos de ensamble (Random Forest, Gradient Boosting), es probable que puedan reducir aún más el RMSE, pero eso queda como trabajo futuro fuera del alcance de este entregable. Lo que queda demostrado es que **el Modelo 1 es la mejor opción entre las tres alternativas evaluadas en este proyecto**.

---

*Proyecto desarrollado para el curso CD-501 Analítica Predictiva de Datos —
Universidad Fidélitas.*
